import { MatrixGreenTextDirective } from './matrix-green-text.directive';

describe('MatrixGreenTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MatrixGreenTextDirective();
    expect(directive).toBeTruthy();
  });
});
